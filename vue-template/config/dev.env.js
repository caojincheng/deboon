module.exports = {
    NODE_ENV: '"development"',
	//如果是本地启动vue前端服务，则需要在host里配置vue.deppontest.com域名为127.0.0.1
	//后端service服务也需要在host里配置 dpboot.deppontest.com域名为127.0.0.1
    BASE_API: '"http://dpboot.deppontest.com:8080/vue-service/"',
}
